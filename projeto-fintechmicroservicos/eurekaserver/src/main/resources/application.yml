spring:
  application:
    name: eurekaserver
  security:
    basic:
      enabled: true
    user:
      name: fintech-eureka-user
      password: Magnamon2025

server:
  port: 8761

eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
